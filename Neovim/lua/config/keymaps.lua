-- ==============================================================================
-- LEADER KEY CONFIGURATION
-- ==============================================================================
-- Set leader keys before loading any plugins or keymaps that reference them
-- The leader key must be set before lazy.nvim loads plugins
vim.g.mapleader = " "               -- Set leader key to spacebar
vim.g.maplocalleader = "\\"         -- Set local leader key to backslash